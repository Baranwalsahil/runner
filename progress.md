# Task Progress

- [x] complete: tasks-01-frontend-scaffold — done 2026-05-16. Vite+React+Tailwind v3 scaffold. 22/22 vitest tests pass. Dev server serves "TERRITORY RUN" with text-primary-fixed (#c3f400) Lexend on dark grid bg. glass-panel/neon-border-lime/neon-border-cyan/hex-mesh utilities present.
- [x] complete: tasks-02-shared-layout — done 2026-05-16. react-router-dom 7. Components: Icon, TopNavBar (NavLink active = lime border-b-2), AlertBar (conditional on message), Footer, Fab (navigates via prop), AppLayout (Outlet + chrome, AlertBar only on /dashboard). 4 routes wired. 45/45 vitest pass. All 4 routes return HTTP 200.
- [x] complete: tasks-03-landing-page — done 2026-05-16. Hero/FeatureGrid/MapPreview/CtaBanner ported from landing_page.html. CTAs route to /dashboard, /battlefield. Added .hex-grid CSS utility. AppLayout main relaxed to `pt-28 pb-xl` for full-bleed hero. 61/61 vitest pass. Curl confirms modules + compiled CSS.
- [x] complete: tasks-04-player-dashboard — done 2026-05-16. TerritoryDominance (7-bar mock chart), QuickRunStats, TerritoryMapPreview (static img+HUD overlays+zoom), RecentBattlesFeed (4 initial+8 extra, load-more swaps to "End of History" + check icon + disabled, scrollIntoView). AlertBar shows on /dashboard. 83/83 vitest pass. Curl 200 all modules.
- [x] complete: tasks-05-battlefield-map — done 2026-05-16. maplibre-gl + h3-js installed. lib/h3Utils (cellToBoundary + cellsToGeoJSON), lib/mapStyle (OSM raster + dark paint), data/mockCells (gridDisk k=4 around Seattle res 9, 4 owner colors). Components: MapCanvas (GeoJSON source/fill/line layers, click handler, setData on cells change), MapHud (live battles, zoom/locate/layers, legend), CellDetailPanel (h3Index/owner/ownership/CHALLENGE), PlayersOnline. Battlefield.jsx wires all + selectedCell state. maplibre-gl mocked in tests via __mocks__. 108/108 vitest pass. Curl 200 all modules; maplibre-gl.css imported into index.css.
- [x] complete: tasks-06-global-leaderboard — done 2026-05-16. data/mockLeaderboard (50 generated + current user, sorted by cells, deterministic). Components: Podium (top-3 asymmetric w/ CHAMPION badge), RankTable (sortable cells/area/streak, 10/page prev/next, highlights currentUserId), FilterChips (region + time, aria-pressed). Leaderboard.jsx wires filter+sort state. 136/136 vitest pass. Curl 200 all modules.
- [x] complete: tasks-07-backend-scaffold — done 2026-05-16. FastAPI scaffold in server/ (Python 3.13 venv). config (pydantic-settings), logging (structlog), errors (validation + http + fallback), middleware (request logger), routers/health. 6/6 pytest pass. uvicorn :8000 returns {"status":"ok","uptime":...,"version":"0.1.0"}; CORS allows localhost:5173, rejects others; 404 fallback clean.
- [x] complete: tasks-08-db-schema — done 2026-05-19. asyncpg 0.31 in server reqs. app/db/pool.py (lazy singleton, statement_cache_size=0 for PgBouncer txn mode, fetch/fetchrow/execute/transaction helpers, close_pool). app/db/schema.sql + migrations/001_init.sql (idempotent: postgis + pgcrypto extensions, users/runs/claimed_cells tables, GIST idx_runs_trace, btree user/started_at/claimed_at/cells_user indexes, DO-block ALTER PUBLICATION supabase_realtime ADD TABLE claimed_cells guarded). scripts/migrate.py CLI (sorted migrations/*.sql, schema_migrations tracking table, BEGIN/COMMIT per file, DATABASE_URL_DIRECT → DATABASE_URL fallback, dotenv-loaded). shared/constants.{py,js} (H3_RESOLUTION=9, OWNER_PALETTE x6, MAX_SPEED_MPS=12, MAX_RUN_HOURS=4, MAX_CELLS_PER_RUN=2000, GPS_ACCURACY_THRESHOLD_M=50). config.py adds database_url_direct (optional). main.py lifespan opens/closes pool with try/except. Makefile `make migrate` target. .env.example documents DATABASE_URL_DIRECT. 9/9 pytest pass against local postgis docker (6 baseline + test_db_pool [2] + test_migrate [1]); 3 integration tests skip cleanly when TEST_DATABASE_URL unset. Verified docker postgis:16-3.4 → migrate creates extensions, all 4 tables, GIST index on runs.gps_trace; 2nd run prints `skip 001_init.sql`.
- [x] complete: tasks-09-auth-jwt — done 2026-05-19. BE: bcrypt 5.0 + python-jose 3.5 + email-validator (dropped passlib due to bcrypt 5 incompat). app/services/auth_service.py (hash_password w/ 72-byte guard, verify_password, issue_token HS256, decode_token raises 401), user_service.py (create_user / get_user_by_email returning (User, hash) / get_user_by_id, UserAlreadyExists on unique violation). schemas/auth.py (SignupRequest min_len=8 + username regex, LoginRequest, User, AuthResponse). deps.py (get_db_pool + get_current_user). routers/auth.py (POST /auth/signup 201, /auth/login 200 generic 401 "Invalid credentials", GET /auth/me, POST /auth/logout 204). main.py registers auth router. FE: lib/auth.js (saveToken/getToken/clearToken/apiBaseUrl/apiFetch attaches Bearer + 401 → clearToken+assign /auth, apiJson throws Error with message). hooks/useAuth.js. components/auth/AuthProvider.jsx (hydrates via /auth/me on mount, signIn/signUp/signOut). ProtectedRoute (loading spinner / Navigate to /auth). SignInForm + SignUpForm + Auth route w/ tab toggle + Navigate to /dashboard when user. App.jsx wraps AuthProvider, /dashboard /battlefield /leaderboard wrapped in ProtectedRoute, /auth public. 27/27 pytest pass against postgis docker (8 auth_service unit + 10 auth_routes integration + 9 baseline); fixture order fixed via explicit dep on _env. 156/156 vitest pass (20 new: 10 auth-lib + 3 ProtectedRoute + 2 SignInForm + 2 SignUpForm + 4 Auth). Live E2E verified: signup → 201 + token, login → 200, me w/ token → 200, me w/o → 401, login wrong pw → 401 "Invalid credentials", logout → 204, CORS preflight from :5173 → 200. All 8 FE auth modules compile via Vite (HTTP 200).
- [x] complete: tasks-10-runs-api — done 2026-05-19. BE: h3==4.4.2 installed. app/constants.py mirrors shared/constants.py. schemas/run.py (Point w/ accuracy + timestamp; RunCreate min_length=2 + ended_at>started_at + MAX_RUN_HOURS=4 guard; RunResult; RunSummary). services/h3_service.trace_to_cells (h3.latlng_to_cell, dedupe set). services/gps_filter.haversine_m + filter_trace (drops accuracy>50m and walks segments dropping speed>12 m/s candidates; missing timestamps skip speed check) + trace_distance_m. services/run_service.ingest_run (filter → cells → cap 2000 → ST_GeomFromText LINESTRING → executemany CLAIM_SQL with ON CONFLICT WHERE user_id IS DISTINCT FROM EXCLUDED → recompute users.total_cells = COUNT(*)) + list_runs + get_run. routers/runs (POST 201, GET list, GET by id 404 across users). main.py registers runs router. errors.py wraps validation details via jsonable_encoder (pydantic v2 ctx ValueError no longer crashes). FE: hooks/useGeolocation.js (watchPosition wrapper, defensive on missing navigator on cleanup/stop, start/stop/clear). components/run/RunTracker.jsx (Start/Stop UI, live timer, distance via haversine, points count, est cells, error rendering, posts via apiJson to /runs, shows result panel + nav /dashboard). routes/Run.jsx. App.jsx adds /run protected. AppLayout FAB now navigates /run. 47/47 pytest pass against postgis docker (24 unit + 23 integration: 10 runs + 10 auth + 3 db); 168/168 vitest pass (12 new: 7 useGeolocation + 5 RunTracker). Live E2E: signup → token; POST /runs → 201 {run_id, cells_claimed:1, new_total:1}; GET /runs → run row with distance_meters≈22.24; no-auth POST → 401; DB shows 1 run + 1 claimed_cell + total_cells incremented.
- [x] complete: tasks-11-territory-leaderboard-api — done 2026-05-19. BE: schemas/territory.py (Bounds w/ parse_csv + range checks, CellOut, TerritoryStats); schemas/leaderboard.py (LeaderboardRow, LeaderboardPage); services/color.py (lru_cache md5 → OWNER_PALETTE idx, OWNER_PALETTE added to app/constants); services/territory_service (h3.LatLngPoly + h3shape_to_cells candidates, WHERE h3_index = ANY($1::text[]) JOIN users for username, color_for_uuid per row, cells_in_bounds + cells_for_user + stats); services/leaderboard_service (top w/ ROW_NUMBER OVER ORDER BY total_cells DESC + period weekly/daily via INTERVAL claimed_at filter; nearby via CTE + ABS(rank-me.rank), qualified columns to avoid ambiguous "rank"); routers/territory (GET /territory?bounds, /territory/stats, /territory/user/{id}) + routers/leaderboard (GET /leaderboard, /leaderboard/nearby) — auth-required, registered in main.py. FE: lib/api.js (territory.list w/ adaptApiCell, territory.byUser, territory.stats, leaderboard.top, leaderboard.nearby); hooks/useTerritory (500ms debounce on bounds change, returns {cells, loading, error}); routes/Battlefield wired to useTerritory + map moveend updates bounds (Seattle default), shows status overlay; routes/Leaderboard fetches via lbApi.top, adapts row to existing Podium/RankTable shape, time filter → period mapping, loading + empty + error states; routes/Dashboard fetches user's recent cells via territory.byUser and feeds RecentBattlesFeed via initialBattles/extraBattles props (mock fallback retained). Tests: 72/72 pytest (9 new unit: 4 color + 5 bounds; 16 new integration: 8 territory router + 8 leaderboard router; rest baseline). 177/177 vitest (existing 168 + 5 useTerritory + 4 api-lib; rewrote Leaderboard.test/Dashboard.test/App.test to wrap in AuthContext via test/utils/withAuth.jsx and stub fetch). Live E2E: alpha+beta signup → A run claims 2 cells, B run claims 1; /territory bbox → 2 cells w/ username+color; stats={total_cells:2,total_users:2,contested_cells:1}; /leaderboard sorted alpha rank 1; /leaderboard?period=weekly works; /leaderboard/nearby returns both; bad bounds → 422; unauth → 401. All FE modules served 200 by Vite.
- [x] complete: tasks-12-realtime-cache — done 2026-05-19. BE: redis 7.4 + hiredis 3.3 + fakeredis 2.35 in server reqs. app/cache/redis_client.py (lazy singleton, NullCache fallback when REDIS_URL unset, exposes get/set/setex/delete/zadd/zrange/zrevrange/zcard/scan_iter/close). cache/leaderboard_cache (ZSET "leaderboard:global" upsert_user_total + top_ids w/ ZREVRANGE WITHSCORES + total_users). cache/territory_cache (bbox_key quantized to 3 decimals → ~110m, set_bbox with TTL_SECONDS=10 via SETEX, get_bbox deserializes JSON to CellOut list, flush_all scans "territory:*"). services/run_service.ingest_run now captures displaced owners pre-upsert, batch UPDATE users.total_cells via ANY($1::uuid[]) RETURNING, ZADDs every affected user score + flush_all territory. services/territory_service.cells_in_bounds gets cache param: try get_bbox first, fall through to DB then set_bbox. services/leaderboard_service.top period=all reads ZSET first via top_ids; if non-empty, hydrate usernames via `SELECT FROM users WHERE id = ANY($1::uuid[])`. routers/runs + routers/territory + routers/leaderboard plumb get_cache_client Dep. main.py lifespan calls close_cache. FE: hooks/usePolling.js (generic timer + visibilitychange pause/resume + immediate refire on focus). hooks/useTerritoryPolling (15s; intervalMs=0 when no bounds). hooks/useLeaderboardPolling (30s). routes/Battlefield switched useTerritory → useTerritoryPolling; routes/Leaderboard switched single-fetch → useLeaderboardPolling. Deleted obsolete hooks/useTerritory.js + its test. 88/88 pytest pass against postgis+redis (11 new unit: 3 redis_null + 4 leaderboard_cache + 4 territory_cache; 5 new integration in test_cache_invalidation: ZSET populated by claim, ownership transfer drops displaced score to 0, /territory populates bbox key, 2nd claim flushes territory:*, /leaderboard served from ZSET when DB users.total_cells=0). 185/185 vitest pass (6 usePolling: immediate fire, intervalMs ticks, pauses on hidden, resumes+immediate on visible, unmount cleanup, intervalMs=0 noop; 4 useTerritoryPolling: null bounds = no fetch, fires immediately, 15s interval, refetch on bounds change; 2 useLeaderboardPolling: immediate + 30s, refetch on period change). Live E2E w/ docker redis: POST /runs → ZSET score 1; GET /territory → key "territory:47.590:-122.360:47.630:-122.300" created; 2nd /runs → KEYS territory:* empty; ZSET score 2; /leaderboard returns ZSET data even after UPDATE users SET total_cells=0 (proves cache-hit, not DB fallback).
- [ ] running: tasks-13-deploy — started 2026-05-19, scope swapped from cloud (Vercel + Render) to local docker-compose: compose.yml (db postgis + redis + migrate + server FastAPI + client Vite) + server/Dockerfile + client/Dockerfile + dockerignores + README quickstart
